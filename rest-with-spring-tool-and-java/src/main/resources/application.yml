spring:
  application:
    name: rest-with-spring-tool-and-java
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/my_database?useTimezone=true&serverTimezone=UTC
    username: root
    password: Isaias@1
  jpa:
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
    show-sql: false
    open-in-view: false
logging:
  level:
    br.com.muniz: INFO
spring-doc:
  paths-to-match: /api/**/v1/**
  swagger-ui:
    use-root-patch: true
