cors:
  originPatterns: http://localhost:8080,http://localhost:3000,https://www.udemy.com
spring:
  application:
    name: rest-with-spring-tool-and-java
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
    show-sql: false
    open-in-view: false
logging:
  level:
    br.com.muniz: INFO
spring-doc:
  paths-to-match: /api/**/v1/**
  swagger-ui:
    use-root-patch: true